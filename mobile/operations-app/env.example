# Variante de l'application (dev ou prod)
APP_VARIANT=prod
EXPO_PUBLIC_APP_ENV=production

# ========== DÉVELOPPEMENT LOCAL (web 8081) ==========
# Copier ces lignes dans .env pour développer en local :
# EXPO_PUBLIC_API_URL=http://localhost:5000
# EXPO_PUBLIC_BACKEND_PORT=5000
# Backend requis : docker compose up -d api (ou start-local-minimal.ps1)
# CORS : FLASK_CONFIG=development ou SOCKETIO_CORS_ORIGINS avec http://localhost:8081

# Configuration API (production par défaut)
EXPO_PUBLIC_API_URL=https://api.lirie.ch
EXPO_PUBLIC_BACKEND_PORT=5000

# Configuration Socket.IO
# Production: REQUIS - URL HTTPS
EXPO_PUBLIC_SOCKET_URL=https://api.lirie.ch
# Development: Optionnel - fallback automatique
# Android emulator: http://10.0.2.2:5000
# iOS simulator / Web: http://localhost:5000

# ⚠️ IMPORTANT: Remplacez ces clés par vos vraies clés API
# Ne committez JAMAIS le fichier .env avec les vraies clés
EXPO_PUBLIC_GOOGLE_API_KEY=changeme-google-rest-api-key
EXPO_PUBLIC_ANDROID_MAPS_API_KEY=changeme-android-maps-api-key

# Fichiers de configuration Google Services (optionnel)
# GOOGLE_SERVICES_JSON=./google-services.json
# GOOGLE_SERVICES_PLIST=./GoogleService-Info.plist

# Numéros de build (optionnel, auto-incrémentés en production)
# ANDROID_VERSION_CODE=1
# IOS_BUILD_NUMBER=1

# Forensic auth : logs "refresh_token timeline" (jamais écrit / effacé / illisible)
# Activer uniquement pour debug ; n'expose pas les tokens.
# EXPO_PUBLIC_DEBUG_AUTH=1

# ========== Perf LIRIE / localisation (optionnel — défauts dans le code) ==========
# Seuil « ACK batch socket stale » (ms) pour repli HTTP / garde tâche fond — défaut 75000.
# EXPO_PUBLIC_LOCATION_SOCKET_STALE_MS=75000
#
# Cadence GPS policy (trackingPolicy / locationTracker) — défaut fast 5000 / slow 60000 ms.
# EXPO_PUBLIC_GPS_FAST_MS=5000
# EXPO_PUBLIC_GPS_SLOW_MS=60000
#
# Heartbeat présence (arrière-plan) — défaut activé ; mettre "false" pour désactiver.
# EXPO_PUBLIC_LOCATION_PRESENCE_HEARTBEAT_ENABLED=true
#
# Carte entreprise : resynchro HTTP si WebSocket silencieux > N ms (voir useEnterpriseDriverTracking).
# EXPO_PUBLIC_MAP_SILENCE_RESYNC_MS=
# Endpoint drivers/live côté API (si activé côté backend).
# EXPO_PUBLIC_DRIVERS_LIVE_API=
#
# Sentry (optionnel, aligné app/_layout).
# EXPO_PUBLIC_SENTRY_DSN=

# Ingest / télémétrie (debug Cursor/agent, port 7242)
# En dev : désactivé par défaut pour éviter ERR_CONNECTION_REFUSED si aucun service n'écoute.
EXPO_PUBLIC_DISABLE_INGEST=true
# Pour activer l'ingest en dev (service à l'écoute sur 7242) :
# EXPO_PUBLIC_DISABLE_INGEST=false
# URL complète ou base (optionnel) : Web = localhost, Android emulator = 10.0.2.2, device = IP LAN
# EXPO_PUBLIC_INGEST_URL=http://localhost:7242
# EXPO_PUBLIC_INGEST_PORT=7242
# EXPO_PUBLIC_INGEST_ENDPOINT_ID=5d8025f1-2a4d-4796-97fe-faa80ad8db74

