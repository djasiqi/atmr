/**
 * Cadences GPS / flush — source unique pour syncEngine, trackingPolicy et locationTask.
 */

export const DEFAULT_LOCATION_FLUSH_INTERVAL_MS = 15000;
export const DEFAULT_MISSION_HEARTBEAT_MS = 60000;
export const DEFAULT_PRESENCE_HEARTBEAT_MS = 180000;
