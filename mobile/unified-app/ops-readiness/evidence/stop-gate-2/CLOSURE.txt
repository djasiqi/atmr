STOP GATE #2 — CLOSURE — 2026-06-10 16:45

VERDICT: PASS

Note evidence logcat (2026-06-19): IPv4 publiques redigees en [REDACTED_IPV4] avant versionnement (scan check_no_production_leaks).

CRITERE OFFICIEL (atteint):
  Acceptation disclosure -> FGS actif -> Notification persistante.

PREUVE DE NIVEAU SYSTEME ANDROID (2026-06-10 16:06, S23 SM-S911B, build production-apk
v112 commit f3d22fe9, avec override runtime tracking_background_enabled=true):
  ServiceRecord{ch.liri.operations/expo.modules.location.services.LocationTaskService}
    isForeground=true  foregroundId=481756  types=0x00000008 (FOREGROUND_SERVICE_TYPE_LOCATION)
    foregroundNoti flags=NO_CLEAR|FOREGROUND_SERVICE color=0xff0a7f59 category=service
    createTime stable > 8 min, persiste a travers cycles background/foreground.
  Notification persistante:
    android.title = "Lirie Unified est active"
    android.text  = "Disponibilite active - localisation en cours"
  Preuves: fgs-active-after-flag-override.txt, diag-06-relaunch-flagon.png,
           diag-07-after-continuer-flagon.png, diag-08-notification-shade.png.

ROOT CAUSE:
  tracking_background_enabled=false
    -> guard en tete de ensureNativeTrackingWhileForeground()
    -> chemin FGS jamais tente (startLocationUpdatesAsync jamais appele)
    -> aucun ServiceRecord, aucune notification, fgs_running=false.
  Ce N'EST PAS un bug du moteur de tracking ni un refus Android 14+.
  Le flag derive de EXPO_PUBLIC_ENABLE_BG_LOCATION, non inline a "1" dans le build
  production-apk malgre sa presence dans eas.json ET l'environnement EAS dashboard.
  Preuve in-app (panneau QA, diag-04-missions-deeplink.png):
    BG flag: no | Native error: tracking_background_enabled=false.
  Detail complet: fgs-diagnostic-rootcause.md.

VALIDATION:
  Code Android      : OK (FGS demarre des que le flag est true)
  Android 14+       : OK (FOREGROUND_SERVICE_TYPE_LOCATION accorde)
  Expo Location     : OK (LocationTaskService foreground stable)
  Notification      : OK (NO_CLEAR, libelle conforme disclosure)

MITIGATION PRODUCTION (en place, conservee):
  Backend prod /srv/atmr/.env.production:
    MOBILE_FEATURE_FLAGS={"tracking_background_enabled":true}
  Override runtime via bootstrap feature_flags -> gagne sur l'env build.
  A conserver jusqu'a ce qu'un build embarque correctement le flag sans override.

POINTS PASSES (rappel):
  [x] Build EAS prod AAB + APK ; APK installe S23
  [x] AASA / assetlinks / privacy HTTP 200 ; deep links activate/reset
  [x] Backend DELETE compte + token revoke (prod)
  [x] Privacy client + chauffeur captures
  [x] Presence HORS fenetre 07h-19h (code + runtime)
  [x] FGS presence en fenetre — refus disclosure => pas de FGS / pas de notif
  [x] FGS presence en fenetre — acceptation disclosure => FGS + notif persistante (PREUVE SYSTEME)

SUITE:
  - Ticket separe BUILD-EXPO-001 : audit inlining EXPO_PUBLIC_* (BG_LOCATION, DRIVER_PUSH, ...).
    Probleme de chaine de build EAS, hors scope conformite Play. Voir BUILD-EXPO-001.md.
  - Passer a STOP GATE Release : video BG location, captures disclosure, justification BG,
    export Play Console, soumission.
