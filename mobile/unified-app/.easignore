# Ne pas envoyer les .env locaux (dev LAN) — les secrets prod viennent d'EAS / eas.json.
.env
.env.*
!.env.example

# Ne jamais réutiliser un dossier android/ local (prebuild EAS doit être propre).
android/
ios/
