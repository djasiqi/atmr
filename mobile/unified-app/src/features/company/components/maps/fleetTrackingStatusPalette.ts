/**
 * Palette statuts tracking flotte — alignée web (S1.6 / S3).
 * Source unique mobile pour les hex partagés avec le portail entreprise.
 */

/** Orange constrained — identique à CONSTRAINED_MARKER_COLOR web (#f97316). */
export const FLEET_CONSTRAINED_FILL = "#f97316";

/** Dernière position connue (non active KPI). */
export const FLEET_LAST_KNOWN_FILL = "#cbd5e1";

/** Pause / break — distinct du constrained. */
export const FLEET_BREAK_FILL = "#F59E0B";
