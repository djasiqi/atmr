/** Fenêtre de consolidation des updates GPS avant publication React. */
export const REALTIME_FLUSH_MS = 500;
/** Âge max d'un batch en attente avant flush forcé (anti-starvation). */
export const MAX_BATCH_AGE_MS = 1_000;
