/**
 * Fichier d’entree TypeScript. Metro embarque `VoiceMessageBar.web` (web) ou
 * `VoiceMessageBar.native` (iOS / Android) — pas ce re-export, sauf en secours.
 */
export { VoiceMessageBar } from "./VoiceMessageBar.native";
