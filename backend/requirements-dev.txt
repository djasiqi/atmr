# Requirements pour le développement et les tests
# Ces dépendances ne sont PAS installées dans l'image de production
#
# Structure recommandée : inclure base + prod + outils de dev
# Pour du dev RL léger, vous pouvez ajouter : -r requirements-rl-no-torch.txt

-r requirements.base.txt
-r requirements.prod.txt

# Tests
pytest>=8.0.0
pytest-flask>=1.3.0
freezegun>=1.2.0  # Mock du temps pour tests déterministes
pypdf>=4.0.0  # Extraction texte PDF pour STOP GATE S2 (HEADER/LINES)
pdfminer.six>=20221105  # Analyse bbox pied de page facture

# Outils de développement (optionnel, peut être dans requirements.txt si utilisé en prod)
# factory-boy>=3.3.0  # Déjà dans requirements.base.txt car utilisé en production
# rich>=13.0.0  # Déjà dans requirements.base.txt car utilisé en production

