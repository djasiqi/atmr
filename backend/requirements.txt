# backend/requirements.txt
# Alias par défaut vers requirements.prod.txt
# 
# Ce fichier est conservé pour compatibilité avec les outils qui attendent requirements.txt
# Il pointe vers la configuration de production (légère, sans RL)

-r requirements.prod.txt