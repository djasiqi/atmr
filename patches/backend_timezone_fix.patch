--- a/backend/services/invoice_service.py
+++ b/backend/services/invoice_service.py
@@ -1,4 +1,4 @@
-from datetime import datetime, timedelta
+from datetime import datetime, timedelta, timezone
 from sqlalchemy import and_
 from models import (
     db, Invoice, InvoiceLine, InvoiceReminder,
@@ -108,9 +108,9 @@
                 subtotal_amount=subtotal,
                 total_amount=subtotal,
                 balance_due=subtotal,
-                issued_at=datetime.utcnow(),
-                due_date=datetime.utcnow() + timedelta(days=billing_settings.payment_terms_days or 30),
+                issued_at=datetime.now(timezone.utc),
+                due_date=datetime.now(timezone.utc) + timedelta(days=billing_settings.payment_terms_days or 30),
                 status=InvoiceStatus.DRAFT
             )
@@ -261,7 +261,7 @@
                 invoice_id=invoice_id,
                 level=level,
                 added_fee=fee_amount,
-                generated_at=datetime.utcnow()
+                generated_at=datetime.now(timezone.utc)
             )
             
             db.session.add(reminder)
@@ -286,7 +286,7 @@
             
             # Mettre à jour le niveau de rappel
             invoice.reminder_level = level
-            invoice.last_reminder_at = datetime.utcnow()
+            invoice.last_reminder_at = datetime.now(timezone.utc)
             
             # Générer le PDF du rappel
             pdf_url = self.pdf_service.generate_reminder_pdf(invoice, level)
@@ -369,7 +369,7 @@
         try:
             overdue_invoices = Invoice.query.filter(
                 and_(
                     Invoice.status.in_([InvoiceStatus.SENT, InvoiceStatus.PARTIALLY_PAID]),
                     Invoice.balance_due > 0,
-                    Invoice.due_date < datetime.utcnow()
+                    Invoice.due_date < datetime.now(timezone.utc)
                 )
             ).all()
@@ -433,7 +433,7 @@
             # Factures éligibles pour le 1er rappel
             if '1' in schedule_days:
                 days_after_due = schedule_days['1']
-                cutoff_date = datetime.utcnow() - timedelta(days=days_after_due)
+                cutoff_date = datetime.now(timezone.utc) - timedelta(days=days_after_due)
                 
                 invoices_for_reminder1 = Invoice.query.filter(
                     and_(
@@ -450,7 +450,7 @@
             # Factures éligibles pour le 2e rappel
             if '2' in schedule_days:
                 days_after_reminder1 = schedule_days['2']
-                cutoff_date = datetime.utcnow() - timedelta(days=days_after_reminder1)
+                cutoff_date = datetime.now(timezone.utc) - timedelta(days=days_after_reminder1)
                 
                 invoices_for_reminder2 = Invoice.query.filter(
                     and_(
@@ -467,7 +467,7 @@
             # Factures éligibles pour le 3e rappel
             if '3' in schedule_days:
                 days_after_reminder2 = schedule_days['3']
-                cutoff_date = datetime.utcnow() - timedelta(days=days_after_reminder2)
+                cutoff_date = datetime.now(timezone.utc) - timedelta(days=days_after_reminder2)
                 
                 invoices_for_reminder3 = Invoice.query.filter(
                     and_(

--- a/backend/tasks/billing_tasks.py
+++ b/backend/tasks/billing_tasks.py
@@ -1,5 +1,5 @@
 import logging
 from celery import Celery
-from datetime import datetime, timedelta
+from datetime import datetime, timedelta, timezone
 from models import db, Invoice, InvoiceStatus, CompanyBillingSettings
 from services.invoice_service import InvoiceService
@@ -46,7 +46,7 @@
         notification_service = NotificationService()
         
         # Récupérer les rappels générés aujourd'hui qui n'ont pas encore été envoyés
-        today = datetime.utcnow().date()
+        today = datetime.now(timezone.utc).date()
         reminders_to_send = db.session.query(InvoiceReminder).filter(
             InvoiceReminder.generated_at >= today,
             InvoiceReminder.sent_at.is_(None),
@@ -59,7 +59,7 @@
                 notification_service.send_reminder_notification(reminder)
                 
                 # Marquer comme envoyé
-                reminder.sent_at = datetime.utcnow()
+                reminder.sent_at = datetime.now(timezone.utc)
                 db.session.commit()
                 
                 app_logger.info(f"Notification de rappel envoyée pour la facture {reminder.invoice.invoice_number}")
@@ -84,7 +84,7 @@
         invoice_service = InvoiceService()
         
         # Calculer la période précédente
-        now = datetime.utcnow()
+        now = datetime.now(timezone.utc)
         if now.month == 1:
             period_year = now.year - 1
             period_month = 12
@@ -157,7 +157,7 @@
         app_logger.info("Début du nettoyage des anciennes factures")
         
         # Factures payées depuis plus de 7 ans
-        cutoff_date = datetime.utcnow() - timedelta(days=7*365)
+        cutoff_date = datetime.now(timezone.utc) - timedelta(days=7*365)
         
         old_paid_invoices = db.session.query(Invoice).filter(
             Invoice.status == InvoiceStatus.PAID,
@@ -194,7 +194,7 @@
         notification_service = NotificationService()
         
         # Calculer la période précédente
-        now = datetime.utcnow()
+        now = datetime.now(timezone.utc)
         if now.month == 1:
             period_year = now.year - 1
             period_month = 12

