# Autopsie terrain — DRIVER-7514 / mission 39067

**Date** : 2 septembre 2026  
**Extraction** : 2026-09-05 · Postgres prod via Docker (DLE, health, booking, assignment)  
**Code GPS** : non modifié

## Verdict du cas

| Question | Réponse |
| -------- | ------- |
| Trou intra-mission de 279 min ? | **Non au sens tracking continu** |
| Que s’est-il passé ? | Démarrage mission avec **2 points**, flap FGS + tempête health, puis **silence total** (GPS + heartbeats) pendant toute la course et après |
| Ce cas explique-t-il le p99 LIVE 117 s ? | **Non** — 0 point LIVE de 7514 avec lag ≥ 60 s sur 7 j |
| Ce cas explique-t-il les 1 859 PUT 429 ? | **Non** — 7514 est le chauffeur le moins bursty ; le 2 sept il n’a presque pas PUT |
| Faux LIVE / mission stale ? | **Oui, à 19:28 UTC** : 1 point `mission_live` + `mission_id=39067` **après** `COMPLETED` |
| Hotfix GPS maintenant | **Non** — cas mobile / session / attribution, à recouper sur le device |

```text
7514 / 39067

14:48 → 14:49
BOOT / BOARDING INSTABLE
fgs_not_running storm
2 positions seulement

14:49 → 16:48
MOBILE SILENCE
course réellement active
0 GPS
0 heartbeat

16:48
BOOKING COMPLETED
assignment encore ONBOARD

16:48 → 19:28
MISSION STATE ORPHAN
plus de course réelle
mission_id encore attaché côté mobile/state

19:28
ZOMBIE RECOVERY
nouvelle session
faux mission_live
anti_zombie + stale fallback restart

GPS CAPTURE / MOBILE       incident confirmé
GPS INGEST / REDIS         aucune faute démontrée
GPS FANOUT                 aucune faute démontrée
MISSION LIFECYCLE          incohérence confirmée
p99 contributor            NO
429 contributor            NO
```

---

## Chronologie minute par minute (UTC, +2 = Zurich)

Mission 39067 : transport patient, aller simple, `COMPLETED`.  
Planifié 16:30 (heure naive stockée, cohérente avec 16:30 Zurich).  
`boarded_at` 14:49:11 UTC (16:49 ZH) · `completed_at` 16:48:31 UTC (18:48 ZH).  
Durée réelle à bord ≈ **1 h 59**.

Assignment `8892` : `actual_pickup_at` = boarded, **`actual_dropoff_at` NULL**, statut resté **`ONBOARD`** (révision 4) alors que le booking est terminé.

### 12:08 — mission précédente 39066

Même chauffeur, même journée. **Un seul** DLE LIVE (`seq=1`, HTTP) à 12:08:47. Booking 39066 completed 13:27 UTC. Même signature : pas de trace pendant la course.

Health 12:01 : `fix_stale` / `tracking_active=false`, FGS déjà up. 12:08 : tracking ON, flap `fgs_not_running` quelques secondes, recovery.

### 14:48:21–14:49:05 — démarrage 39067 (la minute utile)

| UTC | GPS | Health / session |
| --- | --- | ---------------- |
| 14:48:21 | — | App `active`, `tracking_active=false`, FGS ON |
| 14:48:30 | **DLE #1** LIVE HTTP seq=1 session `…juzb2qua` · acc 3,8 m · ~Nyon (46.383, 6.224) · lag persist 30 s | Session 2431 démarre |
| 14:48:35 | — | `no_location_fix` (alors qu’un fix vient d’être persisté) |
| 14:48:37–14:48:45 | — | **Tempête** `fgs_not_running` + bascules `active`/`background` (~10 événements / seconde) |
| 14:48:43 | — | Retour `no_location_fix` / FGS ON / tracking OFF |
| 14:48:54 | **DLE #2** LIVE HTTP · 24 s plus tard · 30 m plus loin · lag 12,5 s | Dernier GPS de la course |
| 14:49:00 | — | FGS recovered, `health_monitor_ok`, `last_fix_age` 0–4 s |
| 14:49:11 | — | Booking **boarded** |
| 14:49:05 | — | **Dernier heartbeat avant le trou** |

174 heartbeats dans l’heure 14:00 UTC, presque tous comprimés dans ces 45 secondes. Device : **Samsung SM-S911B**, Android, app **1.0.13**, `battery_optimized=false`.

### 14:49:06 → 16:48:31 — course réelle, silence

- **0 DLE** (tous modes, toutes missions)
- **0 heartbeat**
- Booking passe à `COMPLETED` à 16:48:31 UTC **sans aucun point GPS intermédiaire**

Ce n’est pas un chauffeur qui « disparaît 8 minutes puis revient ». C’est une **session morte dès le départ** après le flap FGS.

### 16:48:31 → 19:28:14 — après course

Toujours 0 GPS / 0 health. Le « trou 279 min » du soak mélange :

1. **~2 h 00** de mission sans GPS (14:48:54 → 16:48:31) — vrai trou métier  
2. **~2 h 40** après `COMPLETED` (16:48:31 → 19:28:14) — plus une mission

### 19:28:14–19:28:18 — reprise

| UTC | Fait |
| --- | ---- |
| 19:28:14 | **DLE #3** LIVE HTTP seq=1 **nouvelle** session `…4qimhcu9` · lag 4,6 s · 22,4 km plus au sud (Genève) · **`mission_id` encore 39067** |
| 19:28:16 | Health : `fix_stale`, `last_fix_age_seconds=6429` (~107 min), trigger `anti_zombie_fix_stale` |
| 19:28:18 | `stale_fallback_restart` → `tracking_active=true`, FGS ON, âge fix 2 s |

`6429 s` avant 19:28:16 ⇒ le téléphone croit avoir eu un fix vers **17:41 UTC**. Ce fix **n’est jamais arrivé** en DLE. File locale / process mort / réseau, pas un 429 (aucune rafale acceptée).

---

## Ce que ce n’est pas

- **Pas un rate-limit** : 7514 a au plus 1 point accepté / 10 s le 2 sept ; 91 paires ≤ 2 s sur 7 j (vs 32 k pour DRIVER-4).
- **Pas une dédup** : 3 `event_id` distincts, 2 sessions, `seq=1` à chaque redémarrage.
- **Pas une panne backend globale** : le 2 sept, 28 957 points flotte ont été persistés. Seul 7514 est silencieux (6 points la journée vs 2 024 le 30 août).
- **Pas le moteur du p99** : voir ci-dessous.

---

## p99 117 s — pas ce trou, une queue diffuse

Points LIVE avec `created_at - recorded_at ≥ 60 s` / 7 j :

| driver_id | LIVE | lag ≥ 60 s | lag ≥ 100 s | part ≥ 60 s |
| --------- | ---: | ---------: | ----------: | ----------: |
| 7755 | 24 358 | 6 109 | 1 515 | **25,1 %** |
| 4 | 24 847 | 5 441 | 1 774 | **21,9 %** |
| 23345 | 10 470 | 1 046 | 538 | 10,0 % |
| 3 | 839 | 9 | 1 | 1,1 % |
| 16150 | 1 207 | 5 | 0 | 0,4 % |
| **7514** | **622** | **0** | **0** | **0 %** |

Le p99 vient d’une **dégradation diffuse** sur les deux gros volumes (4 et 7755) : files locales / flush tardif, pas d’un petit nombre de trous 7514.

---

## 429 PUT — distribution (hors ce cas)

Limiteur HTTP (défauts, env non surchargés) : **30 / 10 s** et **120 / 60 s**.  
WS batch prod : **12 / 5 s** (plus serré que le runbook historique 2/10 s).

PUT `driver_driver_location` 429 par jour (Zurich) :

| Jour | ≈ 429 |
| ---- | ----: |
| 29 août | 2 |
| 31 août | 92 |
| 1 sept | 68 |
| 2 sept | 308 |
| **3 sept** | **1 094** |
| 4 sept | 270 |

Pic : **3 sept 11:00–14:00 ZH** (~190 + 172 + 160 + 162 / h).  
Le 2 sept, le pic 429 est **13:00 ZH** (148) — autour de 39066, pas du trou 14:48–19:28 UTC.

Sans label `driver_id` sur `http_requests_total`, on ne peut pas attribuer les 429 à un chauffeur. L’hypothèse utile : **flush / double voie HTTP des gros volumes (4, 7755)**, pas 7514. Ne pas changer les fréquences tant que ce n’est pas confirmé (logs `HTTP driver location rate limit driver_id=` — absents depuis le restart du 5 sept).

---

## Risques métier de ce cas

1. **Carte aveugle pendant ~2 h de course réelle** — le trou « intra-mission » utile est 14:48:54 → 16:48:31, pas 279 min.
2. **Point 19:28 encore `mission_live` + mission 39067** après `COMPLETED` + assignment resté `ONBOARD` → candidat **faux LIVE**.
3. **Health monitor storm** au start (centaines de POST en 45 s) : dette mobile, pas un chantier GPS proto.
4. Pattern 7514 : journées LIVE très creuses (2 sept = 4 LIVE) alors que PRESENCE peut être dense (30 août = 1 625).

---

## Clôture moteur GPS (2026-09-05)

Ce cas est **sorti du moteur GPS**. Il ne justifie aucune modification de cadence, Redis, fanout, Kafka ou rate limiter.

```text
7514   → chantier séparé : MOBILE CONTINUITY / FGS / OS LIFECYCLE
39067  → chantier séparé : BOOKING + ASSIGNMENT + MOBILE STATE CLEANUP
         gate PASS : 0 faux mission_live après COMPLETED
```

p99 et 429 sont tranchés ailleurs (FIFO iOS ; 429 = bord de fenêtre). Soak : [`gps-prod-soak-7d-2026-09-05.md`](gps-prod-soak-7d-2026-09-05.md).
