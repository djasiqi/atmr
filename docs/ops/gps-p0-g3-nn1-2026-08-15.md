# G3 — Compatibilité N / N-1 (`286737a2`)

```text
DATE                         = 2026-08-15
TIP FONCTIONNEL              = 286737a2362eb1e38013c72d04be23fcd608210e
WORKTREE G3                  = C:\Users\jasiq\atmr.worktrees\gps-p0-g3-nn1
BRANCHE RELEASE              = release/gps-p0-2026-08-15  (INCHANGÉE @ 286737a2)
G4 / G5 / TAG / PUSH / BUILD / DEPLOY / ALEMBIC / PURGE = NO-GO
```

## Matrice

| Cas | Client | Backend | Résultat | Preuves |
|-----|--------|---------|----------|---------|
| G3.1 | mobile P0 (`286737a2`) | `286737a2` | **PASS** | Jest 72 PASS (lifecycle, auth, ledger, http202, OBS, heartbeat) ; pytest ledger+health 34+ PASS |
| G3.2 | mobile N-1 / prod (`927640a0`) | `286737a2` | **PASS** | IDs complets ≠ `ids_missing` ; payload `generation=null` → 422 sûr (pas 500, claim libéré, pas HOL serveur) ; health legacy OK |
| G3.3 | vieux client `generation=null` | `286737a2` | **PASS** | T1/T7 + `test_g3_3_*` : HTTP 422, `invalid_ledger_ids`, `retryable=false`, claim absent |
| G3.4 | mobile P0 | backend prod `927640a0` | **DEGRADED-SAFE** | voir § G3.4 |
| G3.5 | session/headless ancien / P0-B | `286737a2` | **PASS** | `trackingAuthPresence` 100% client-only ; 0 diff `backend/routes/auth*` ; tests login/refresh/logout/cross-driver |
| G3.6 | health legacy (sans nouveaux champs) | `286737a2` | **PASS** | schema EXCLUDE + ingest fallback `last_fix`→`location_fix`, `native_last`→`task_invoke` |

```text
G3.1 P0 mobile ↔ release backend      PASS
G3.2 N-1 mobile ↔ release backend     PASS
G3.3 old generation=null              PASS
G3.4 P0 mobile ↔ old backend          DEGRADED-SAFE
G3.5 legacy auth/headless             PASS
G3.6 legacy health payload            PASS

G3 GLOBAL = VERT ✅
```

## Critères bloquants

```text
old client → backend release
  pas de 500                    ✅ (422 Option B)
  pas de claim orphelin         ✅ (release)
  pas de double persistence     ✅ (T3/T5)
  pas de HOL permanent          ✅ (rejected non-retryable ; vs HOL 200 sur prod)

legacy health → backend release
  accepté                       ✅
  fallback legacy               ✅

new client → old backend
  résultat documenté            ✅ (§ G3.4)
  aucune corruption de données  ✅
```

## G3.4 — mobile P0 × backend `927640a0`

Comportement **documenté** (pas de corruption) :

| Surface | Comportement rollback backend-only |
|---------|-------------------------------------|
| Ledger happy path (READY + gen + seq) | Inchangé : persist sync comme avant |
| `generation=null` | **Ne survient plus** côté mobile P0 (gate READY / `dropEnqueueObserved`) |
| Si IDs incomplets quand même | Contrat **prod** : 200 `ingested_non_persisted` / `ledger_ids_missing` (HOL possible) — Option B absente |
| Device-health | Champs OBS **ignorés** (`strict=False` / unknown EXCLUDE ; ingest lit seulement clés connues) — **pas d’exception** |
| P0-B présence tracking | **Aucune API auth nouvelle** — login/refresh/logout OK sur backend actuel |
| Observability classes | Absentes côté serveur prod — dégradation monitoring, pas data plane |

```text
BACKEND-ONLY ROLLBACK SAFE = YES

Implication G4 :
  rollback backend/workers seulement = AUTORISABLE
  rollback mobile simultané = NON REQUIS pour sûreté data
  (observabilité ages/classes et Option B 422 absentes jusqu’au re-deploy backend)
```

Verdict G3.4 = **DEGRADED-SAFE** (pas PASS « feature-parity », mais **sûr** pour data / auth / health).

## P0-B

```text
trackingAuthPresence = client-only (SecureStore)
backend auth delta 927640a0..286737a2 = vide
→ pas de dépendance backend nouvelle pour login/refresh/logout/headless
```

## Tests exécutés (overlay Docker TIP, host Jest TIP)

```text
pytest :
  test_ledger_server_claim_lifecycle_p0c.py
  test_location_persisted_sync_p0e.py
  test_location_event_claim_release_p02.py
  test_driver_device_health.py
  test_g3_nn1_compat.py
  → PASS (après fix G3.6 EXCLUDE)

Jest :
  nativeTrackingLifecycle | trackingAuthPresence | ledgerClient
  http202AckSemantics | trackingObservability* | deviceHealthHeartbeat
  → 72 PASS / 7 suites
```

## Freeze

```text
G0 = VERT ✅
G1 = VERT ✅
G2 = VERT ✅
G3 = VERT ✅

G4 = NO-GO (prochaine gate)
G5 = NO-GO

TAG / PUSH / BUILD / DEPLOY / ALEMBIC / PURGE = NO-GO
release/gps-p0-2026-08-15 tip = 286737a2 (aucun commit dessus)
```

```text
✅ **Implémenté** : G3 N/N-1 validé sur TIP `286737a2` ; BACKEND-ONLY ROLLBACK SAFE=YES.
**Reste à faire** : GO G4 (rollback réel + anti-skew) uniquement.
```
