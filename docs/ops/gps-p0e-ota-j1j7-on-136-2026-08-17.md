# P0-E — OTA diag J1→J7 sur binaire 136 (pas de build 137)

```text
GO BUILD 137          = NON ⛔
GO OTA SUR 136        = OUI ✅
natif P0→P8           = déjà dans 136
JS J1→J7              = OTA only (observationnel)
runtimeVersion        = 1.0.12 (identique 136)
channel               = production
platform              = android
```

## Compatibilité (pré-publish ✅)

| Check | Binaire 136 | Update |
|-------|-------------|--------|
| runtimeVersion | `1.0.12` | `1.0.12` |
| channel | `production` (`production-apk` → production) | `--channel production` |
| platform | android | android only |
| comportement Location | inchangé | logs `ATMR_JS_J` only |

## EAS Update

```text
group ID     = 1ab4f5d9-ded9-4cfe-8f47-cf4c291ee71e
android ID   = 01a0113a-3806-7fa3-ac98-4c73f77ad277
message      = P0-E A4b: J1-J7 observational diag on 136 (no behavior change)
dashboard    = https://expo.dev/accounts/drinjasiqi/projects/operations-app/updates/1ab4f5d9-ded9-4cfe-8f47-cf4c291ee71e
```

## Procédure device (1 run FG)

1. Confirmer **versionCode 136** installé (`ch.liri.operations`).
2. **Force-stop** puis rouvrir l’app (chargement OTA `ON_LOAD`).
3. Optionnel : vérifier update chargée (écran / logs Expo Updates).
4. Session chauffeur + mission active (témoin typique 20135 / 38243).
5. Logcat parallèle :

```powershell
adb logcat -s ATMR_LTC_P:I ReactNativeJS:V | findstr "ATMR_LTC_P ATMR_JS_J"
```

6. FG 60–90 s + déplacement léger.
7. Corrélation figée — **stop au premier trou** :

```text
P8 executeTask JS=true
↓
J1 TASK_ENTER
↓
J2 LOCATION_SELECTED
↓
J3 CONTEXT/GATE DECISION
↓
J4 SESSION / MISSION
↓
J5 PAYLOAD_FROZEN
↓
J6 ENQUEUE
↓
J7 FLUSH
```

## Lecture rapide

| Observation | Verdict |
|-------------|---------|
| P8 sans J1 | JS task non atteint / bundle OTA non chargé |
| J1 puis stop J2 | pas de location dans le payload task |
| J3 rejected | gate JS (reason dans log) |
| J6 inserted=false | enqueue drop (reason) |
| J7 sent=0 + J6 ok | flush / réseau / ack path |
| chaîne complète + DLE>0 | A4b résolu côté capture ; revoir serveur/PUT |

## Run FG — FAIT

→ [`gps-p0e-ota-j1j7-136-fg-verdict-2026-08-17.md`](./gps-p0e-ota-j1j7-136-fg-verdict-2026-08-17.md)

```text
P8→J6          = PASS (event_id neufs, inserted=true, corrélés P5)
J7             = flush sent>0 mais backend_acked=0 + last_eid ANCIEN + depth~290
DLE            = 0
FIRST_STOP     = J7_FLUSH_NO_ACK_NEW
A4b enqueue    = CLOSED ✅
```

## Hors scope

Build 137 · PLAY · Q1 · UX offline · serveur · HOME
