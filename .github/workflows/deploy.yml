# yaml-language-server: disable
name: Build & Deploy

# ⚠️ Workflow automatique DÉSACTIVÉ - Déclenchement manuel uniquement
on:
  workflow_dispatch: # Déclenchement manuel uniquement
    inputs:
      additional_tag:
        description: "Alias Docker optionnel (ex: latest, v1.2.0). Jamais la source de vérité — le tag canonique est toujours sha-<12>"
        required: false
        default: ""
      skip_deploy:
        description: "Ne pas déployer sur le serveur (seulement build + push)"
        type: boolean
        required: false
        default: false
      no_cache:
        description: "Build sans cache (pour corriger flask_limiter.storage, etc.)"
        type: boolean
        required: false
        default: false

concurrency:
  group: atmr-production-deployment
  cancel-in-progress: false

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  gps-mobile-critical:
    name: GPS mobile critical (preflight)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile/unified-app
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v7
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: mobile/unified-app/package-lock.json
      - run: npm ci
      - run: npm run typecheck:tracking
      - run: npm run test:gps-critical

  build-and-push:
    name: Build & Push Production Image
    needs: [gps-mobile-critical]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
      DOCKER_IMAGE: ${{ secrets.DOCKER_IMAGE }}
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

    outputs:
      push_success: ${{ steps.verify_push.outputs.success }}
      docker_tag: ${{ steps.meta.outputs.docker_tag }}
      source_sha: ${{ steps.meta.outputs.source_sha }}
      backend_image_digest: ${{ steps.build_backend.outputs.digest }}
      ws_service_image_digest: ${{ steps.build_ws.outputs.digest }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Compute canonical tag + validate additional_tag
        id: meta
        run: |
          set -euo pipefail
          SOURCE_SHA="${GITHUB_SHA}"
          DOCKER_TAG="sha-${SOURCE_SHA:0:12}"
          ADDITIONAL_TAG="${{ github.event.inputs.additional_tag }}"
          if [ -n "${ADDITIONAL_TAG}" ] && \
             [[ ! "${ADDITIONAL_TAG}" =~ ^[A-Za-z0-9_][A-Za-z0-9_.-]{0,127}$ ]]; then
            echo "additional_tag Docker invalide: ${ADDITIONAL_TAG}"
            exit 1
          fi
          echo "docker_tag=${DOCKER_TAG}" >> "${GITHUB_OUTPUT}"
          echo "source_sha=${SOURCE_SHA}" >> "${GITHUB_OUTPUT}"
          echo "additional_tag=${ADDITIONAL_TAG}" >> "${GITHUB_OUTPUT}"
          echo "DOCKER_TAG=${DOCKER_TAG}" >> "${GITHUB_ENV}"
          echo "SOURCE_SHA=${SOURCE_SHA}" >> "${GITHUB_ENV}"
          echo "Tag canonique: ${DOCKER_TAG}"
          echo "SOURCE_SHA=${SOURCE_SHA}"

      - name: Validate required secrets
        run: |
          echo "🔍 Validation des secrets requis..."
          MISSING_SECRETS=()

          if [ -z "${{ secrets.DOCKER_IMAGE }}" ]; then
            MISSING_SECRETS+=("DOCKER_IMAGE")
          else
            echo "✅ DOCKER_IMAGE: ${{ secrets.DOCKER_IMAGE }}"
          fi

          echo "✅ DOCKER_TAG (canonique): ${DOCKER_TAG}"

          if [ -z "${{ secrets.DOCKERHUB_USERNAME }}" ]; then
            MISSING_SECRETS+=("DOCKERHUB_USERNAME")
          else
            echo "✅ DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}"
          fi

          if [ -z "${{ secrets.DOCKERHUB_TOKEN }}" ]; then
            MISSING_SECRETS+=("DOCKERHUB_TOKEN")
          else
            echo "✅ DOCKERHUB_TOKEN: [masqué]"
          fi

          if [ ${#MISSING_SECRETS[@]} -ne 0 ]; then
            echo ""
            echo "❌ Secrets manquants ou vides: ${MISSING_SECRETS[*]}"
            exit 1
          fi

          echo ""
          echo "✅ Tous les secrets requis sont présents et non vides"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        continue-on-error: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Clean up disk space
        run: |
          echo "🧹 Nettoyage de l'espace disque avant le build..."
          docker system prune -af --volumes || true
          rm -rf ~/.cache/pip || true
          df -h

      - name: Generate SBOM with Trivy
        uses: aquasecurity/trivy-action@0.35.0
        with:
          scan-type: "fs"
          scan-ref: "./backend"
          format: "cyclonedx"
          output: "sbom-cyclonedx.json"
          exit-code: "0"
      - name: Ensure SBOM artefact produced
        run: test -s sbom-cyclonedx.json

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Calculate requirements hash
        id: requirements_hash
        run: |
          if [ -f backend/requirements.prod.txt ]; then
            REQUIREMENTS_HASH=$(sha256sum backend/requirements.prod.txt backend/requirements.base.txt 2>/dev/null | sha256sum | cut -d' ' -f1)
            echo "requirements_hash=${REQUIREMENTS_HASH}" >> $GITHUB_OUTPUT
            echo "📋 Hash de requirements.prod.txt + requirements.base.txt: ${REQUIREMENTS_HASH:0:12}..."
          elif [ -f backend/requirements.txt ]; then
            REQUIREMENTS_HASH=$(sha256sum backend/requirements.txt | cut -d' ' -f1)
            echo "requirements_hash=${REQUIREMENTS_HASH}" >> $GITHUB_OUTPUT
            echo "📋 Hash de requirements.txt (fallback): ${REQUIREMENTS_HASH:0:12}..."
          else
            echo "⚠️  requirements.prod.txt et requirements.txt non trouvés"
            echo "requirements_hash=missing" >> $GITHUB_OUTPUT
          fi

      - name: Prepare backend image tags
        id: backend_tags
        run: |
          set -euo pipefail
          TAGS="${DOCKER_IMAGE}:${DOCKER_TAG}"
          if [ -n "${{ steps.meta.outputs.additional_tag }}" ]; then
            TAGS="${TAGS}
          ${DOCKER_IMAGE}:${{ steps.meta.outputs.additional_tag }}"
          fi
          {
            echo "tags<<EOF"
            echo "${TAGS}"
            echo "EOF"
          } >> "${GITHUB_OUTPUT}"

      - name: Build & push backend image
        id: build_backend
        uses: docker/build-push-action@v7
        with:
          context: ./backend
          file: ./backend/Dockerfile.production
          target: runtime
          build-args: |
            WITH_RL=false
            RL_ENABLED=false
            REQUIREMENTS_HASH=${{ steps.requirements_hash.outputs.requirements_hash }}
          push: true
          tags: ${{ steps.backend_tags.outputs.tags }}
          labels: |
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          no-cache: ${{ github.event.inputs.no_cache == 'true' }}

      - name: Derive ws-service Docker image name
        id: ws_service_image
        env:
          DOCKER_IMAGE_REF: ${{ secrets.DOCKER_IMAGE }}
        run: |
          DI="${DOCKER_IMAGE_REF}"
          case "$DI" in
            *atmr-backend) WS="${DI/%atmr-backend/atmr-ws-service}" ;;
            *) WS="docker.io/djasiqi/atmr-ws-service" ;;
          esac
          echo "name=${WS}" >> "$GITHUB_OUTPUT"
          echo "📦 WS_SERVICE_IMAGE dérivé pour push CI"

      - name: Prepare ws-service image tags
        id: ws_tags
        run: |
          set -euo pipefail
          WS="${{ steps.ws_service_image.outputs.name }}"
          TAGS="${WS}:${DOCKER_TAG}"
          if [ -n "${{ steps.meta.outputs.additional_tag }}" ]; then
            TAGS="${TAGS}
          ${WS}:${{ steps.meta.outputs.additional_tag }}"
          fi
          {
            echo "tags<<EOF"
            echo "${TAGS}"
            echo "EOF"
          } >> "${GITHUB_OUTPUT}"

      - name: Build & push ws-service image
        id: build_ws
        uses: docker/build-push-action@v7
        with:
          context: ./services/ws-service
          file: ./services/ws-service/Dockerfile
          push: true
          tags: ${{ steps.ws_tags.outputs.tags }}
          labels: |
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          no-cache: ${{ github.event.inputs.no_cache == 'true' }}

      - name: Verify ws-service image was pushed
        run: |
          WS_TAG="${{ steps.ws_service_image.outputs.name }}:${{ env.DOCKER_TAG }}"
          for i in {1..5}; do
            if docker manifest inspect "${WS_TAG}" > /dev/null 2>&1; then
              echo "✅ Image ws-service ${WS_TAG} trouvée sur Docker Hub"
              exit 0
            fi
            echo "  Tentative $i/5: ws-service non visible, attente 5s..."
            sleep 5
          done
          echo "❌ Image ws-service ${WS_TAG} introuvable sur Docker Hub"
          exit 1

      - name: Verify image was pushed
        id: verify_push
        run: |
          echo "🔍 Vérification que l'image a bien été poussée..."
          IMAGE_TAG="${{ env.DOCKER_IMAGE }}:${{ env.DOCKER_TAG }}"
          echo "Image attendue: ${IMAGE_TAG}"

          for i in {1..5}; do
            if docker manifest inspect "${IMAGE_TAG}" > /dev/null 2>&1; then
              echo "✅ Image ${IMAGE_TAG} trouvée sur Docker Hub"
              echo "success=true" >> $GITHUB_OUTPUT
              exit 0
            fi
            echo "  Tentative $i/5: Image non trouvée, attente 5 secondes..."
            sleep 5
          done

          echo "❌ L'image ${IMAGE_TAG} n'a pas été trouvée sur Docker Hub après 5 tentatives"
          echo "success=false" >> $GITHUB_OUTPUT
          if [ "${{ github.event.inputs.skip_deploy }}" != "true" ]; then
            echo "⚠️  Le déploiement ne s'exécutera pas car l'image n'existe pas"
            exit 1
          else
            echo "ℹ️  Déploiement ignoré (skip_deploy=true), mais l'image n'a pas été trouvée"
          fi

      - name: Publish Kafka P0 handoff summary
        run: |
          set -euo pipefail
          BACKEND_DIGEST="${{ steps.build_backend.outputs.digest }}"
          {
            echo "## Déploiement Kafka P0"
            echo
            echo "- Source SHA: \`${GITHUB_SHA}\`"
            echo "- Backend tag: \`${DOCKER_TAG}\`"
            echo "- Backend digest: \`${BACKEND_DIGEST}\`"
            echo
            echo "Inputs workflow Deploy Kafka P0 :"
            echo "- \`source_sha\` = \`${GITHUB_SHA}\`"
            echo "- \`image_digest\` = \`${BACKEND_DIGEST}\`"
          } >> "${GITHUB_STEP_SUMMARY}"
          echo "SOURCE_SHA=${GITHUB_SHA}"
          echo "BACKEND_IMAGE_DIGEST=${BACKEND_DIGEST}"
          echo "DOCKER_TAG=${DOCKER_TAG}"

      - name: Scan image with Trivy (CRITICAL fixable bloque)
        uses: aquasecurity/trivy-action@0.35.0
        with:
          image-ref: ${{ env.DOCKER_IMAGE }}:${{ env.DOCKER_TAG }}
          format: table
          exit-code: "1"
          ignore-unfixed: true
          vuln-type: os,library
          severity: CRITICAL
          scanners: vuln
          trivyignores: .trivyignore

      - name: Build release manifest
        id: release_manifest
        run: |
          set -euo pipefail
          chmod +x scripts/release/build_release_manifest.sh
          BACKEND_DIGEST="${{ steps.build_backend.outputs.digest }}"
          WS_DIGEST="${{ steps.build_ws.outputs.digest }}"
          WS_REPO="${DOCKER_IMAGE/%atmr-backend/atmr-ws-service}"
          SOURCE_SHA="${SOURCE_SHA}" \
          BACKEND_REPO="${DOCKER_IMAGE}" \
          BACKEND_DIGEST="${BACKEND_DIGEST}" \
          WS_REPO="${WS_REPO}" \
          WS_DIGEST="${WS_DIGEST}" \
          OUT_FILE=release-manifest.json \
            ./scripts/release/build_release_manifest.sh
          echo "manifest_path=release-manifest.json" >> "$GITHUB_OUTPUT"

      - name: Upload release manifest
        uses: actions/upload-artifact@v4
        with:
          name: release-manifest
          path: release-manifest.json

  deploy:
    name: Deploy Production Environment
    needs: [build-and-push]
    if: |
      github.event.inputs.skip_deploy != 'true' &&
      needs.build-and-push.outputs.push_success == 'true'
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: read
    env:
      DOCKER_IMAGE: ${{ secrets.DOCKER_IMAGE }}
      DOCKER_TAG: ${{ needs.build-and-push.outputs.docker_tag }}
      SSH_HOST: ${{ secrets.SSH_HOST }}
      SSH_USER: ${{ secrets.SSH_USER }}
      SSH_PORT: ${{ secrets.SSH_PORT }}
      SSH_KEY: ${{ secrets.SSH_KEY }}
      APP_ENCRYPTION_KEY_B64: ${{ secrets.APP_ENCRYPTION_KEY_B64 }}
      SECRET_KEY: ${{ secrets.SECRET_KEY }}
      JWT_SECRET_KEY: ${{ secrets.JWT_SECRET_KEY }}
      POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
      POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
      POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
      REDIS_PASSWORD: ${{ secrets.REDIS_PASSWORD }}
      MAIL_PASSWORD: ${{ secrets.MAIL_PASSWORD }}
      SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
      PDF_BASE_URL: ${{ secrets.PDF_BASE_URL }}
      GOOGLE_MAPS_API_KEY: ${{ secrets.GOOGLE_MAPS_API_KEY }}
      MASTER_ENCRYPTION_KEY: ${{ secrets.MASTER_ENCRYPTION_KEY }}
      GRAFANA_ADMIN_USER: ${{ secrets.GRAFANA_ADMIN_USER }}
      GRAFANA_ADMIN_PASSWORD: ${{ secrets.GRAFANA_ADMIN_PASSWORD }}
      GRAFANA_ROOT_URL: ${{ secrets.GRAFANA_ROOT_URL }}
      SMTP_HOST: ${{ secrets.SMTP_HOST }}
      SMTP_PORT: ${{ secrets.SMTP_PORT }}
      SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
      SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
      ALERTMANAGER_FROM_EMAIL: ${{ secrets.ALERTMANAGER_FROM_EMAIL }}
      ALERT_EMAIL_TO: ${{ secrets.ALERT_EMAIL_TO }}
      SOCKETIO_CORS_ORIGINS: ${{ secrets.SOCKETIO_CORS_ORIGINS }}
      BREVO_API_KEY: ${{ secrets.BREVO_API_KEY }}
      SMS_NOTIFICATIONS_ENABLED: ${{ secrets.SMS_NOTIFICATIONS_ENABLED }}
      TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID }}
      TWILIO_AUTH_TOKEN: ${{ secrets.TWILIO_AUTH_TOKEN }}
      TWILIO_PHONE_NUMBER: ${{ secrets.TWILIO_PHONE_NUMBER }}
      POSTGRES_HOST: ${{ secrets.POSTGRES_HOST }}
      FLASK_ENV: ${{ secrets.FLASK_ENV }}
      FLASK_CONFIG: ${{ secrets.FLASK_CONFIG }}
      ENVIRONMENT: ${{ secrets.ENVIRONMENT }}
      FIREBASE_SERVICE_ACCOUNT_B64: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_B64 }}
      INTERNAL_SERVICE_TOKEN: ${{ secrets.INTERNAL_SERVICE_TOKEN }}
      INTERNAL_SERVICE_TOKEN_NEXT: ${{ secrets.INTERNAL_SERVICE_TOKEN_NEXT }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Validate required secrets
        run: |
          REQUIRED_SECRETS=(
            "SSH_HOST"
            "SSH_USER"
            "SSH_KEY"
            "SSH_PORT"
            "APP_ENCRYPTION_KEY_B64"
            "SECRET_KEY"
            "JWT_SECRET_KEY"
            "POSTGRES_PASSWORD"
            "POSTGRES_USER"
            "POSTGRES_DB"
            "REDIS_PASSWORD"
            "DOCKER_IMAGE"
            "DOCKER_TAG"
            "SOCKETIO_CORS_ORIGINS"
            "BREVO_API_KEY"
            "POSTGRES_HOST"
            "FLASK_ENV"
            "FLASK_CONFIG"
            "ENVIRONMENT"
            "INTERNAL_SERVICE_TOKEN"
          )
          MISSING_SECRETS=()
          for secret in "${REQUIRED_SECRETS[@]}"; do
            if [ -z "${!secret:-}" ]; then
              MISSING_SECRETS+=("$secret")
            fi
          done
          if [ ${#MISSING_SECRETS[@]} -ne 0 ]; then
            echo "❌ Secrets manquants: ${MISSING_SECRETS[*]}"
            exit 1
          fi
          if [ "${#INTERNAL_SERVICE_TOKEN}" -lt 32 ]; then
            echo "❌ INTERNAL_SERVICE_TOKEN trop court (min 32 caractères, F-01)"
            exit 1
          fi
          echo "✅ Tous les secrets requis sont présents"

      - name: Verify image exists on Docker Hub
        run: |
          echo "🔍 Vérification que l'image existe sur Docker Hub..."
          IMAGE_TAG="${DOCKER_IMAGE}:${DOCKER_TAG}"
          if [ -z "${DOCKER_IMAGE}" ] || [ -z "${DOCKER_TAG}" ]; then
            echo "❌ DOCKER_IMAGE ou DOCKER_TAG vide"
            exit 1
          fi
          echo "Image attendue: ${IMAGE_TAG}"
          for i in {1..5}; do
            if docker manifest inspect "${IMAGE_TAG}" > /dev/null 2>&1; then
              echo "✅ Image ${IMAGE_TAG} trouvée sur Docker Hub"
              exit 0
            fi
            echo "  Tentative $i/5: Image non trouvée, attente 5 secondes..."
            sleep 5
          done
          echo "❌ L'image ${IMAGE_TAG} n'a pas été trouvée sur Docker Hub"
          exit 1

      - name: Copy compose files to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ env.SSH_HOST }}
          username: ${{ env.SSH_USER }}
          key: ${{ env.SSH_KEY }}
          port: ${{ env.SSH_PORT }}
          source: "docker-compose.production.yml,docker-compose.monitoring.yml,docker-compose.kafka.yml,docker-compose.kafka.atmr-network.yml,docker-compose.kafka.p0-hold.yml"
          target: "/srv/atmr"

      - name: Copy monitoring directory to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ env.SSH_HOST }}
          username: ${{ env.SSH_USER }}
          key: ${{ env.SSH_KEY }}
          port: ${{ env.SSH_PORT }}
          source: "monitoring/"
          target: "/srv/atmr/"
          strip_components: 0

      - name: Copy deployment scripts to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ env.SSH_HOST }}
          username: ${{ env.SSH_USER }}
          key: ${{ env.SSH_KEY }}
          port: ${{ env.SSH_PORT }}
          source: "scripts/smoke_tests.sh,scripts/deploy-production.sh,scripts/env.production.defaults.fragment,scripts/lib/kafka_checks.sh,scripts/lib/kafka_topics_init.sh,scripts/deploy-kafka-production.sh,scripts/kafka-init-topics.sh,scripts/ops/validate-prod-correction.sh,scripts/ops/apply-redis-sysctl.sh,scripts/ops-tracking-p0-recreate-ingest.sh,scripts/kafka-env-effective.sh,scripts/check-kafka-production.sh"
          target: "/srv/atmr/"

      - name: Decode Firebase service account locally
        if: env.FIREBASE_SERVICE_ACCOUNT_B64 != ''
        run: |
          set -e
          echo "${FIREBASE_SERVICE_ACCOUNT_B64}" | base64 -d > firebase-service-account.json
          if ! python3 -c "import json; json.load(open('firebase-service-account.json'))"; then
            echo "❌ Firebase service account JSON is invalid after decoding"
            rm -f firebase-service-account.json
            exit 1
          fi
          echo "✅ Firebase service account decoded ($(wc -c < firebase-service-account.json) bytes)"

      - name: Copy Firebase service account to server
        if: env.FIREBASE_SERVICE_ACCOUNT_B64 != ''
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ env.SSH_HOST }}
          username: ${{ env.SSH_USER }}
          key: ${{ env.SSH_KEY }}
          port: ${{ env.SSH_PORT }}
          source: "firebase-service-account.json"
          target: "/srv/atmr"

      - name: Set Firebase file permissions
        if: env.FIREBASE_SERVICE_ACCOUNT_B64 != ''
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ env.SSH_HOST }}
          username: ${{ env.SSH_USER }}
          key: ${{ env.SSH_KEY }}
          port: ${{ env.SSH_PORT }}
          script: |
            install -m 600 /srv/atmr/firebase-service-account.json /srv/atmr/firebase-service-account.json.bak 2>/dev/null || true
            chmod 600 /srv/atmr/firebase-service-account.json
            echo "✅ Firebase service account permissions set (0600)"

      # Deploy via args positionnels (flux valide run #519 / 3e751de).
      # Chemin --env-file + SCP .env retire : scp-action echoue sur les dotfiles.
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ env.SSH_HOST }}
          username: ${{ env.SSH_USER }}
          key: ${{ env.SSH_KEY }}
          port: ${{ env.SSH_PORT }}
          script: |
            chmod +x /srv/atmr/scripts/deploy-production.sh
            chmod +x /srv/atmr/scripts/smoke_tests.sh
            chmod +x /srv/atmr/scripts/ops-tracking-p0-recreate-ingest.sh || true
            chmod +x /srv/atmr/scripts/kafka-env-effective.sh || true
            /srv/atmr/scripts/deploy-production.sh \
              "${{ secrets.APP_ENCRYPTION_KEY_B64 }}" \
              "${{ secrets.SECRET_KEY }}" \
              "${{ secrets.JWT_SECRET_KEY }}" \
              "${{ secrets.POSTGRES_PASSWORD }}" \
              "${{ secrets.POSTGRES_USER }}" \
              "${{ secrets.POSTGRES_DB }}" \
              "${{ secrets.REDIS_PASSWORD }}" \
              "${{ secrets.MAIL_PASSWORD }}" \
              "${{ secrets.SENTRY_DSN }}" \
              "${{ secrets.PDF_BASE_URL }}" \
              "${{ secrets.GOOGLE_MAPS_API_KEY }}" \
              "${{ secrets.MASTER_ENCRYPTION_KEY }}" \
              "${{ env.DOCKER_IMAGE }}" \
              "${{ env.DOCKER_TAG }}" \
              "${{ secrets.GRAFANA_ADMIN_USER }}" \
              "${{ secrets.GRAFANA_ADMIN_PASSWORD }}" \
              "${{ secrets.GRAFANA_ROOT_URL }}" \
              "${{ secrets.SMTP_HOST }}" \
              "${{ secrets.SMTP_PORT }}" \
              "${{ secrets.SMTP_USERNAME }}" \
              "${{ secrets.SMTP_PASSWORD }}" \
              "${{ secrets.ALERTMANAGER_FROM_EMAIL }}" \
              "${{ secrets.ALERT_EMAIL_TO }}" \
              "${{ secrets.SOCKETIO_CORS_ORIGINS }}" \
              "${{ secrets.BREVO_API_KEY }}" \
              "${{ secrets.POSTGRES_HOST }}" \
              "${{ secrets.FLASK_ENV }}" \
              "${{ secrets.FLASK_CONFIG }}" \
              "${{ secrets.ENVIRONMENT }}" \
              "${{ vars.PLATFORM_API_URL_PROD }}" \
              "${{ vars.PLATFORM_LINK_PROMETHEUS }}" \
              "${{ vars.PLATFORM_LINK_ALERTMANAGER }}" \
              "${{ vars.PLATFORM_API_URL_DEMO }}" \
              "${{ secrets.SAFERPAY_CUSTOMER_ID }}" \
              "${{ secrets.SAFERPAY_TERMINAL_ID }}" \
              "${{ secrets.SAFERPAY_API_USERNAME }}" \
              "${{ secrets.SAFERPAY_API_PASSWORD }}" \
              "${{ secrets.SMS_NOTIFICATIONS_ENABLED }}" \
              "${{ secrets.TWILIO_ACCOUNT_SID }}" \
              "${{ secrets.TWILIO_AUTH_TOKEN }}" \
              "${{ secrets.TWILIO_PHONE_NUMBER }}" \
              "${{ secrets.INTERNAL_SERVICE_TOKEN }}" \
              "${{ secrets.INTERNAL_SERVICE_TOKEN_NEXT }}"
