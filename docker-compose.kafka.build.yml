# Override local uniquement — build backend pour consumers Kafka.
# Ne jamais fusionner en production / CI / workflow P0.
# Usage local multi-brokers :
#   docker compose -f docker-compose.yml -f docker-compose.kafka.yml \
#     -f docker-compose.kafka.build.yml up -d --build
services:
  tracking-kafka-consumer:
    build:
      context: ./backend
      dockerfile: Dockerfile

  tracking-processed-fanout:
    build:
      context: ./backend
      dockerfile: Dockerfile

  kafka-dlq-consumer:
    build:
      context: ./backend
      dockerfile: Dockerfile
